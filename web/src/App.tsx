// Placeholder until Phase 5. Exists so the workspace typechecks and `npm run dev`
// brings up both halves from the start.
export function App() {
  return <main className="app">Habit Tracker — frontend lands in Phase 5.</main>;
}
